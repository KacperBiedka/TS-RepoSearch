import { library } from "@fortawesome/fontawesome-svg-core";

import { faChevronDown } from "@fortawesome/free-solid-svg-icons";

// Import icons separately to avoid large bundle size
library.add(
  faChevronDown
  // more icons go here
);
